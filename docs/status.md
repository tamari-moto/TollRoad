# 現在の実装状況

**このファイルだけが時期に依存する。** 機能を足したらここを更新する。
規約（[docs/rules/](rules/)）は実装状況に関わらず有効なので、混ぜないこと。

**M8 まで完了 = ロードマップ完走**（[roadmap.md](roadmap.md) 参照）。
1日目から60日目のランク画面まで、マウスだけで通しで遊べる。市場・積荷・大陸図・
製作所・相場メモ・島と装備・探索・仲間・航海日誌・休息・リザルト。
解像度は 1280x720。
セーブ/ロードは実装済み（日送りで自動保存、開始画面から「続きから」）。

構造の説明は [architecture.md](architecture.md) にある。
規約は [docs/rules/](rules/)、architecture.md は構造の説明という分担。

## 画面

サイドパネルのタブは7つ。`main.gd` の `_tab_strip_buttons` の順に並ぶ:

| # | タブ | パネル |
|---|---|---|
| 1 | 市場 | `MarketPanel` |
| 2 | 積荷 | `CargoPanel` |
| 3 | 製作所 | `製作所` |
| 4 | 相場メモ | `相場メモ` |
| 5 | 島と装備 | `島と装備` |
| 6 | 探索 | `探索` |
| 7 | 仲間 | `CompanionPanel` |

キー操作は `tr_rest`（Space）と `tr_tab_1`〜`7`（1〜7）。
タブを増やしたら `project.godot` の `[input]` にも足すこと。

シルバー・現在地・積載は `HUD`、目標到達度と残り日数・曜日は `GoalPanel` が
担う（別パネル）。日数バーは `GoalPanel` にある。

## 実装済みの機能

**ギルド仲間（同行者）**（2026-08-11）。4人（フィナ/ガドルフ/セラフィーナ/ロッコ）
から同時に1人だけ選んで同行させる。加入・切り替えは無料・即時（日数を消費しない）。
効果は `game_session.gd` が companion id を直接見て適用しており、汎用の効果ディスパッチは
作っていない。詳細は [game_design.md](game_design.md) の「7.3 ギルド仲間（同行者）」。

**都市ごとの在庫と需要**（2026-08-13）。市場は無限ではなく、都市×品目ごとに
在庫（買える上限）と需要（売れる上限）を持ち、毎日その都市の生産量・消費量ぶん補充される。
生産量は `GameData.CITIES` の specialty / bonus から導く（都市ごとの表は持たない）ので、
都市を足しても書き足す必要はない。在庫が薄いと買値が上がり、需要が尽きると売値が下がる。
[market_table.gd](../scripts/systems/market_table.gd) が在庫と需要を持ち、価格へ掛ける
掛け率だけを返す。詳細は [game_design.md](game_design.md) の
「5.3 在庫と需要（生産量・販売数）」。

**物流**（2026-08-14、2026-08-17 に交易路を廃止）。**在庫が自然に増えるのは
生産地だけ**（`PRODUCTION_OTHER_*` は 0）。それ以外の都市の品物は産地から
隊商が運んでくる。

**交易路（`_supply_routes`）は廃止した**（2026-08-17）。産地の在庫を減らさずに
消費地へ毎日補充する層で、品物が湧いて出るため物流のシミュレーションとして
成立していなかった。今は[logistics.gd](../scripts/systems/logistics.gd) の
隊商1層だけで、**質量保存**——降ろした数は必ずどこかの産地から引かれている——が
不変条件になっている（`scenario_m31.gd` の「質量保存」が検査）。

廃止の過程で、交易路が隠していた**構造的な赤字**が出た。装備は1品目につき
産地が1都市しかないのに消費は全都市ぶん積み上がり、生産9個/日 に対し
消費31個/日 だった。物流では埋めようのない差で、交易路が毎日湧かせていた。
`CONSUMPTION_EQUIPMENT` 5→2 / `CONSUMPTION_CAERLEON_EQUIPMENT` 14→5 /
`PRODUCTION_BONUS` 9→16 で釣り合わせてある（詳細は game_data.gd の該当箇所）。
**バランスの実測値が変わったので [rules/balance.md](rules/balance.md) を参照。**

一時的な品切れは**許容する設計**になった。隊商が着くまで4日前後かかるため
棚が尽きる組は出るが、それは物流が効いていることの現れ。検査も「1件も
品切れが無い」ではなく慢性化（全期間ゼロの組が無いこと）で見ている。

**市場デバッグ画面も別ウィンドウにした**（2026-08-17）。F3 の一覧
（[debug_panel.gd](../scripts/ui/debug_panel.gd)）を、下の運送デバッグ画面と
同じく **OS の別ウィンドウ**へ移した（[debug_window.gd](../scripts/ui/debug_window.gd)）。
理由も同じで、パネルが 1080x720 あり既定のビューポート（1280x720）をほぼ覆うため、
在庫を見ながら地図や市場画面を確かめられなかった。

**F5 と違って起動時は閉じている**（`OPEN_ON_START = false`）。F5 は記録が
「開いた瞬間から先しか無い」と困るので開けたままにするが、F3 は押した時点の
在庫を見るだけで遡るものが無い。初期位置も F5 とずらしてある
（`POSITION_OFFSET`）——同じだと両方開いたときに完全に重なる。

パネル自体は単体（オーバーレイ）でも使える形を保っている。「×」は
`close_requested_from_panel` を出し、**繋がれていなければ従来どおり自分を隠す**。
窓に入れたときだけ窓ごと閉じる（パネルだけ隠すと空の窓が残るため）。

**運送デバッグ画面**（2026-08-17）。**OS の別ウィンドウ**として出る
（[logistics_debug_window.gd](../scripts/ui/logistics_debug_window.gd) が置き場所と
開閉、[logistics_debug_panel.gd](../scripts/ui/logistics_debug_panel.gd) が中身）。
**F5** で開閉し、**起動時から開いている**（`OPEN_ON_START`）。

物流は動いている所が画面にほとんど出ない（交易路は地図に出ず、隊商も数本しか
描かれない）ため、値を見ずに壊れ方を判断できなかった。それを見るための画面。

オーバーレイではなく別ウィンドウなのは場所の取り合いのため。パネルは 1180x760 で、
既定のビューポート（1280x720）ではほぼ全面を覆う。物流の調整は
「値をいじる → 日を進める → 地図で荷車を見る」の繰り返しなので、隠し合うと
見比べたい2つが同時に見られない。`Window.force_native`（Godot 4.3 以降）で
**この窓だけ**を native にしている——プロジェクト設定の `embed_subwindows` を
false にすると開始画面とリザルトまで OS のウィンドウになり、本編の見た目が変わる。

この窓に焦点がある間は本編がキーを受け取らないため、**F5（閉じる）と
Space（1日進む）は窓の側でも受け直している**。

4つのビューを持つ:

| ビュー | 中身 |
|---|---|
| 推移 | 走行中・出発・到着の本数と品切れ率の折れ線 |
| 在庫 | 選んだ品目の、棚のある全都市の在庫の折れ線（産地は緑） |
| 流量 | 直近N日に品目ごとに隊商で着いた個数の横棒（交易路は含まない） |
| 立体 | 都市＝在庫の高さの棒、街道＝混み具合の太さ、隊商＝街道上の点 |

右半分は常に**調整欄**で、logistics.gd の const 9項目を実行中だけ上書きできる
（[logistics_tuning.gd](../scripts/systems/logistics_tuning.gd)）。既定値は const の
ままで、上書きを消せば必ず戻る。`SUPPLY_CEILING ≥ ARRIVAL_STOCK_CEILING`
（荷車が消える）や `DAYS_PER_HOP = 1`（荷車が都市の真上にしか出ない）のように
logistics.gd が「そうすると壊れる」と書いている組み合わせは、その場で警告が出る。

記録（[logistics_stats.gd](../scripts/systems/logistics_stats.gd)）はこのパネルが
持ち、**セーブにも RNG にも触らない**。立体ビューの都市配置は大陸図と同じ
`MapView3D.relax_positions()` を使う（配置を別に持つと地理が食い違う）。

都市は10（王国都市9＋レイヴンスパイア）。

**売買アニメーション**（2026-08-18）。市場で売買すると、品目のアイコンが
押したボタンから **HUD まで弧を描いて飛ぶ**。買いは積載、売りはシルバーへ
向かう——その取引で増えるものを指すので、数字を読まなくても飛ぶ向きで
何をした取引か分かる。

以前は市場パネルの内側で完結していた（買う/売るボタン → 同じ行の所持数）。
[fx_layer.gd](../scripts/ui/fx_layer.gd) は最初からパネルをまたぐ想定で
書かれていたのに、層を市場パネルの子として吊っていたため意図が効いておらず、
`SidePanel` の `clip_contents` に切られて外へは出られなかった。今は
[main.gd](../scenes/main/main.gd) が `UI/Root` の直下に1つだけ吊り、
`traded` シグナルの `origin`（それまで受け取るだけで捨てていた）を出発点に
使う。飛び先は HUD の `cargo_anchor()` / `silver_anchor()` が答える。

弧の高さは距離に比例させた（上下限あり）。行程が千px級になり、固定 70px では
直線と見分けがつかなくなったため。

**価格推移グラフ**（2026-08-15）。相場メモに折れ線グラフを追加。横軸が経過日数、
縦軸が価格で、都市を選び品目を複数重ねて表示できる。全都市×全品目の建値を
訪問の有無を問わず毎日記録する [price_history.gd](../scripts/systems/price_history.gd)
が母体で、相場メモ本体（`memo`、訪問した都市だけを記録）とは別の記録として持つ。
セーブにも含まれるが、過去の日数ぶんを再現する手段が無いため、噛み合わない
セーブ（版更新前の古いセーブ・手編集）を読んだ場合は当日の1件だけで作り直す
（`prices` の reroll と同じ「過去は戻せない」制約）。グラフの描画は
[price_chart.gd](../scripts/ui/price_chart.gd)。ゲームに「年」の概念は無いため
横軸は「◯日目」表記のみ。

**街のステータス画面（構想・デバッグ表示のみ）**（2026-08-14）。基礎5資源
（鉱石/木材/繊維/皮/石材）の都市ごとの生産量を五角形のレーダーチャートで
見せる画面の構想スケッチを受け、まずデバッグパネル（F3）の第3ビューとして
実装した。本編のサイドパネル（7タブ）にはまだ出していない。
[city_radar_chart.gd](../scripts/ui/city_radar_chart.gd) が描画、
[debug_panel.gd](../scripts/ui/debug_panel.gd) の「街のステータス」ボタンから
切り替える。満点は特産地（road_distance 0）で最大になる
`GameData.PRODUCTION_SPECIALTY` に正規化してあるので、都市を跨いでも
同じ物差しで形を比較できる。

**大陸図の確認用シーン**（2026-08-15）。`scenes/debug/MapPreview.tscn` を
エディタで開いて F6 で実行すると、大陸図だけが単独で立ち上がる。本編では
大陸図が SubViewport の中に `own_world_3d = true` で作られ、さらにノードが
実行時に `new()` で組まれるため、エディタの 3D ビューポートからは
**原理的に見えない**（別ワールドであり、かつ `.tscn` に存在しない）。
その確認手段として置いた。SubViewport を挟まず `MapView3D` をメインの
世界に直接置いている。

視点は FPS 操作。WASD で移動、マウスで視線、Shift でダッシュ、F で歩く／飛ぶ
の切り替え（飛行中は Q/E で上下）、R で開始位置、F4 でトゥーン、Esc でマウスを
放す。本編の `MapCamera`（周回カメラ）とは座標の決め方が違うため共通化せず、
[map_preview_camera.gd](../scenes/debug/map_preview_camera.gd) を別に置いてある。
地形追従は `terrain_mesh_height_at()` で真下の高さを引くだけで**当たり判定は
無い**（構造物はすり抜ける）。

`GameSession` を持たないので、**都市の選択・隊商・現在地の強調は出ない**。
絵作り（フォグ・グロウ・トゥーン）を見るためのもので、状態が要るなら本編を
起動する。`@tool` は付けていない（`_build()` が重く、エディタごと落ちうる）。

## 検証シナリオ

`scripts/systems/scenario_m1.gd` 〜 `scenario_m32.gd` の32本。
`scenario_all.gd` の `SCENARIO_COUNT` と一致していること（ランナーが照合して止める）。

シナリオの書き方と3層の検証手順は [rules/testing.md](rules/testing.md) を参照。
